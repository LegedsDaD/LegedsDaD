name: EasierBot Assistant

on:
  schedule:
    - cron: '*/15 * * * *'
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  run-bot:
    # Connects to the public repo
    uses: LegedsDaD/EasierBot/.github/workflows/bot-engine.yml@main
    
   
